require "nvchad.options"

-- add yours here!

local o = vim.o
-- o.cursorlineopt ='both' -- to enable cursorline!
return {
  relativenumber = true,  -- Включает относительные номера строк
  number = true,          -- Показывает абсолютный номер текущей строки
}
